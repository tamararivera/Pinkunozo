<% provide(:title, 'Acta del proyecto ' + @project.name) %>
<div>
  <%= render 'projects/header' %>
  <div class="row">
    <div class="col-sm-6 col-sm-offset-3" id="paginator">
      <%= paginate @acts, :remote => true, :theme => 'twitter-bootstrap-3'%>
    </div>
  </div>
  <div id="act">
    <%= render 'act_form'%>
  </div>
</div>