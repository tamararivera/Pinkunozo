<%= form_for(@project,  html: {class: 'form-horizontal'}) do |f| %>
    <div class='form-group'>
      <%= f.label :name, 'Nombre', class: 'col-sm-1 control-label'%>
      <div class='col-sm-11'>
        <%= f.text_field :name, class: 'form-control' %>
      </div>
    </div>
    <div class='form-group'>
      <%= f.label :description, 'Descripción', class: 'col-sm-1 control-label'%>
      <div class='col-sm-11'>
        <%= f.text_area :description, class: 'form-control' %>
      </div>
    </div>
    <div class='form-group'>
      <%= f.label :participants, 'Participantes', class: 'col-sm-1 control-label'%>
      <div class='col-sm-4'>
        <%= f.collection_check_boxes(:participant_ids, User.all, :id, :name) do |b|%>
            <div class='checkbox'>
              <%=b.label(:"data-value" => b.value) { b.check_box(class: 'checkbox-input') + b.text}%>
            </div>
        <%end%>
      </div>
    </div>

    <div class='form-group'>
      <div class='col-sm-12'>
        <button type='submit' class='btn btn-primary'>Guardar</button>
      </div>
    </div>
<% end %>