<% provide(:title, 'Todos los usuarios') %>
<h1>Todos los usuarios</h1>

<%= paginate @users, :theme => 'twitter-bootstrap-3' %>
<ul class="users">
  <%= render @users %>
</ul>
<%= paginate @users, :theme => 'twitter-bootstrap-3' %>